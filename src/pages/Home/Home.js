import React from 'react';
import Child from '../Child';
import Parent from '../Parent';

const Home = () => {
    return (
        <div>
             <Parent />
            
           
        </div>
    );
};

export default Home;